---
import Layout from "../layouts/Layout.astro";
import { GenerateView } from "../components/generate";

// MVP: No auth check for now - will be added with middleware later
// const user = Astro.locals.user;
// if (!user) {
//   return Astro.redirect("/auth/login");
// }
---

<Layout title="Generuj fiszki - 10x Cards">
  <main class="min-h-screen bg-background">
    <div class="container mx-auto py-8">
      <header class="mb-8">
        <h1 class="text-3xl font-bold tracking-tight">Generuj fiszki z AI</h1>
        <p class="mt-2 text-muted-foreground">Wklej tekst, a AI automatycznie wygeneruje dla Ciebie fiszki do nauki</p>
      </header>

      <GenerateView client:load />
    </div>
  </main>
</Layout>
